# Google Colab already includes most of these packages. This file is for local runs.
# Apple Metal 1.2.0 is compatible with TensorFlow through 2.18; keep the
# Apple Silicon stack pinned so a fresh environment can actually load the GPU.
tensorflow==2.18.1; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow-metal==1.2.0; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow>=2.16,<2.21; sys_platform != "darwin" or platform_machine != "arm64"
tensorflow-datasets>=4.9,<5
# TensorFlow 2.18 requires Protobuf 5. The metadata pins avoid loading proto
# files generated by Protobuf 6 into that older runtime on Apple Silicon.
protobuf>=5.29,<6; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow-metadata==1.17.3; sys_platform == "darwin" and platform_machine == "arm64"
googleapis-common-protos==1.66.0; sys_platform == "darwin" and platform_machine == "arm64"
# Protobuf 7 removes FieldDescriptor.label, still used by TFDS 4.9.x.
protobuf>=6.31,<7; sys_platform != "darwin" or platform_machine != "arm64"
importlib-resources>=6; python_version >= "3.11"
numpy>=1.26,<3
pandas>=2.0,<3
jinja2>=3.1,<4
matplotlib>=3.8,<4
seaborn>=0.13,<1
scikit-learn>=1.4,<2
